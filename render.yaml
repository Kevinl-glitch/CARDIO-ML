services:
  - type: web
    name: cardioml-prediction-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
    healthCheckPath: /health
    autoDeploy: true
